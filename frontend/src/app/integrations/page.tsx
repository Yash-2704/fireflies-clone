import { Plug } from "lucide-react";

import { ComingSoon } from "@/components/shell/ComingSoon";

export default function Page() {
  return <ComingSoon title="Integrations" icon={Plug} description="Connect Zoom, Google Meet, your calendar and CRM so meetings are captured automatically." />;
}
